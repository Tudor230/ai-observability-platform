name: backend

on:
  push:
    paths:
      - "backend/**"
      - "shared/**"
      - ".github/workflows/backend.yml"
  pull_request:
    paths:
      - "backend/**"
      - "shared/**"
      - ".github/workflows/backend.yml"

defaults:
  run:
    working-directory: backend

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: aiobs_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 2s
          --health-timeout 2s
          --health-retries 20
    env:
      AIOBS_TEST_DATABASE_URL: postgresql+psycopg://postgres:postgres@localhost:5432/aiobs_test
      AIOBS_E2E_DATABASE_URL: postgresql+psycopg://postgres:postgres@localhost:5432/aiobs_test
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
      - run: uv sync --group dev
      - name: Unit + integration tests (coverage floor 80%)
        run: uv run pytest -q --cov=aiobs_backend --cov-fail-under=80
      - name: E2E smoke (ingest -> rollup -> API reads)
        run: uv run python scripts/e2e_smoke.py

  kpi:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: aiobs_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 2s
          --health-timeout 2s
          --health-retries 20
    env:
      AIOBS_KPI_DATABASE_URL: postgresql+psycopg://postgres:postgres@localhost:5432/postgres
      AIOBS_KPI_PORT: 8000
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
      - name: backend deps
        working-directory: backend
        run: uv sync --group dev
      - name: sdk deps
        working-directory: sdk
        run: uv sync --group dev
      - name: run KPI gate (docs/05)
        working-directory: backend
        run: uv run python scripts/kpi_gate.py
