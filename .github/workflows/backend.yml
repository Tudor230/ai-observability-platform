name: backend

on:
  push:
    paths:
      - "backend/**"
      - "shared/**"
      - ".github/workflows/backend.yml"
  pull_request:
    paths:
      - "backend/**"
      - "shared/**"
      - ".github/workflows/backend.yml"

defaults:
  run:
    working-directory: backend

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: aiobs_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 2s
          --health-timeout 2s
          --health-retries 20
    env:
      AIOBS_TEST_DATABASE_URL: postgresql+psycopg://postgres:postgres@localhost:5432/aiobs_test
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
      - run: uv sync --group dev
      - run: uv run pytest -q

  kpi:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: aiobs_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 2s
          --health-timeout 2s
          --health-retries 20
    env:
      AIOBS_KPI_DATABASE_URL: postgresql+psycopg://postgres:postgres@localhost:5432/postgres
      AIOBS_KPI_PORT: 8000
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
      - name: backend deps
        working-directory: backend
        run: uv sync --group dev
      - name: sdk deps
        working-directory: sdk
        run: uv sync --group dev
      - name: run KPI gate (docs/05)
        working-directory: backend
        run: uv run python scripts/kpi_gate.py
